public class AutomaticRobotSwitch{
    public static void main(String[] args) {
        int switchA == //INPUT GOES HERE, 0 FOR PAUSED LOOP, 1 FOR ACTIVE LOOP//;   
        if (switchA == 1) {
          for (int i == 1; i <//THE NUMBER OF TIMES YOU WANT IT TO GO//; i++) {
          //AFTER THIS USE A ROBOT COMMAND//

 /*

Bro, you have several errors associated with ur code:
1. You can not have dashes in ur class name
2.You can not use switch (I think) because it is a key word
3.I think you meant "==" and not "="

-Jaden

*/

/* 

Fixed them, Removed dashes in both the file and class name and edited the variable. 
The equal sign is intentional and is used to set variables
*/
